if(${WITH_CPP_SUGER})
    add_subdirectory(cplusplus_suger)
endif()

if(${WITH_ALGO})
    add_subdirectory(algorithm)
endif()

if(${WITH_DESIGN_PATTERNS})
    add_subdirectory(design_patterns)
endif()